
<ul>
    <% @articles.each do |article| %>
    <li class = "card">
        <h3> <%= article.title %> </h3>
        <p> <%= article.body %> </p>
        <%= link_to "Read More", article %>
    </li>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
